#include "ScreenCap.h"
#include <stdio.h>



int main(int argv, char *argc) {

	//silly tests
	//int* ptr = (int *)calloc(sizeof(int), 3);
	//ptr[0] = 3;
	//ptr[1] = 3;
	//ptr[2] = 4;
	

	//-------Capture Primary Screen------------------------------------------------------------------------------
	//captures the screen shot from the primary screen
	//captureScreenImageEx(0, "D:\\catured.bmp", 1);

	//-------Get Dominant Color from the primary screen----------------------------------------------------------
	//get the dominant color from the primary screen
	//PColorHistogram colorHist;
	//int hist;
	//int *domCols;
	//getDominantColorFromScreen(2, &colorHist, &hist, &domCols);

	return 0;
}